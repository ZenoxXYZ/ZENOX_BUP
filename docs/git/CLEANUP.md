# Cleanup

After a workstream is closed and its branch is safely merged, the human may remove obsolete local branches and worktrees. Before cleanup, confirm the merge, ensure no local work remains, and preserve any needed evidence.

Never delete branches, worktrees, commits, or history by assumption. Remote-branch deletion and destructive Git operations require explicit human approval.
